namespace Cryptiklemur.RimLogging.Bootstrap;

// TODO(Phase 10): replace stub with real duplicate-assembly detection (Task 10.4)
internal static class DegradedMode
{
    /// <summary>Returns true if another RimLogging assembly is already present and we should run in degraded mode.</summary>
    public static bool AnotherCopyPresent() => false;
}
